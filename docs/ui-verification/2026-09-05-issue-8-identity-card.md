# UI検証依頼: Issue #8 サマリー・カード1（アイデンティティ）

- **依頼日**: 2026-09-05
- **対象ブランチ / コミット**: `feat/issue-8-identity` / `95e25f4`
- **所要時間の目安**: 約 20 分（実機スマホの確認を含む）
- **なぜ逆発注するか**: 実機スマホでの表示・OS のダークモードの実切替・段位色の印象・★グリフの潰れは、いずれもエージェントの環境で再現できない（ブラウザペインは `prefers-color-scheme` のエミュレーションで `matchMedia` の `change` を発火せず、実機の高DPIレンダリングも持たない）。配色と「LLMに作らせた感」の有無はオーナーの決定事項。

> **構成が変わっています（2026-09-05 の是正）**
> 検収でヒーロー高さが未達だったため、**ヒーローは状態によらず固定4行**（名前 / 段位・pt / 進捗バー / 残pt）に限定し、**昇降条件と通算試合数はサマリータブ本体の「段位の詳細」カードへ移しました**。
> ヒーローの高さは 375px 幅・360px 幅とも **290px で一定**（ローディング中・条件成立時も同じ）であることを検収で実測済みです。この手順書は**是正後の構成**に合わせて更新してあります。全項目とも現在のコミット（`95e25f4`）で実施できます。

## 準備

```bash
git switch feat/issue-8-identity
npm run dev
```

> **既知の不具合（本 Issue とは無関係。`main` でも同じ）**: `npm run dev` で `#/4/player/<ID>/summary` を直接開くと、カード1が「データの取得に失敗しました。」になります。開発サーバー（StrictMode）でのみ起きる既存の不具合で、本番ビルドでは正常です。
> **回避策**: `npm run build && npx vite preview` を使い、`http://localhost:4173/` を開いてください。V1・V2・V6 はこちらで実施してください（V3・V4・V5 は `npm run dev` の `#/__identity` で問題ありません）。

> 終わったらサーバーは Ctrl+C で停止してください。

---

## 検証項目

### V1. 実機スマホでのヒーローとスクロールの体感

**操作**

1. PC で `npm run build && npx vite preview -- --host` を実行し、表示される Network の URL（`http://192.168.x.x:4173/`）をメモする。
2. スマホを PC と同じ Wi-Fi に接続し、その URL をスマホのブラウザで開く。
3. 検索画面で適当な名前（例: 「雀」）を入力し、検索結果から1人選んでタップする。
4. サマリー画面が出たら、画面をゆっくり下へスクロールする。
5. 一番下まで行ったら、ゆっくり上へ戻す。

**期待結果**

- 最初の表示で、名前・段位・ポイント・進捗バーと、その下のフィルタのチップが1画面に収まって見える。
- スクロールすると、角丸の白い面（順位グラフの枠がある層）が、上のグレーの帯（名前・段位のある部分）の上に重なりながら覆っていく。
- 上へ戻すと、グレーの帯が再び現れる。

**結果**（← ここを埋めてください）

- [ ] 合格
- [ ] 不合格
- [ ] 判断保留

```
実際に見えたもの / 保留の理由:

```

---

### V2. 段位ごとの配色を、実際のプレイヤー画面で見比べる

**操作**

1. `http://localhost:4173/` を開く。
2. 検索窓に「雀」と入力する。結果に段位（雀傑◯／雀豪◯ など）が出るので、**雀傑の人**を1人タップする。
3. 大きく表示されているポイントの数字と、段位の★の色を見る。
4. ブラウザの戻るで検索画面に戻り、今度は**雀豪の人**を1人タップして同じ場所を見る。
5. 可能なら**雀聖**・**魂天**の人も同様に見る（検索語を「魂」「天」などに変えると見つかりやすい）。

**期待結果**

- 段位ごとに、ポイントの数字と★の色が変わる。
- どの段位でも、数字が背景に埋もれずはっきり読める。

**結果**

- [ ] 合格
- [ ] 不合格
- [ ] 判断保留

```
実際に見えたもの / 保留の理由:

```

---

### V3. `雀傑★★` の★グリフが実機で読めるか

**操作**

1. スマホで `#/__identity`（`npm run dev` のアドレス + `/#/__identity`）を開く。
2. 「4. 雀傑2 四麻」のカードと「5. 雀傑3 昇段目前」のカードを並べて見る。
3. 顔から普段の距離（30cm 程度）で見る。拡大しない。

**期待結果**

- ★が潰れて黒い四角や団子にならず、星の形として見える。
- ★2個（カード4）と★3個（カード5）の違いが、数えなくても一目で分かる。

**結果**

- [ ] 合格
- [ ] 不合格
- [ ] 判断保留

```
実際に見えたもの / 保留の理由:

```

---

### V4. OS のダークモードを実際に切り替えたときの見え方

**操作**

1. PC のブラウザで `http://localhost:5173/#/__identity` を開く。
2. ページ上部の「カラーモード」で **system** を選ぶ。
3. OS の設定でダークモードに切り替える（macOS: システム設定 → 外観 → ダーク／Windows: 設定 → 個人用設定 → 色 → 黒）。
4. ページを見る。**リロードはしない。**
5. OS をライトモードに戻して、もう一度ページを見る。

**期待結果**

- OS を切り替えると、リロードしなくてもページの配色が追随する。
- ダーク側で、「段位の詳細」カードの中の昇降条件（「あと1戦で昇段」「1位」「2位 9,100点以上」など）の文字が背景に埋もれず読める。
- ライトに戻すと元の見え方に戻る。

**結果**

- [ ] 合格
- [ ] 不合格
- [ ] 判断保留

```
実際に見えたもの / 保留の理由:

```

---

### V5. ヒーローの高さが変わらず、層側のカードだけが増減することをどう感じるか

**操作**

1. `http://localhost:5173/#/__identity` を開く。
2. 「4. 雀傑2 四麻（条件なし・常態）」「9. 魂天20（上限なし）」「10. 雀士3」（**「段位の詳細」カードに昇降条件が無い**もの）を見る。
3. 次に「5. 雀傑3 昇段目前」「6. 雀聖3 昇段目前」「7. 雀豪1 降段目前」「8. 魂天1」（**昇降条件がある**もの）を見る。
4. 2 と 3 を何度か行き来して見比べる。
5. ブラウザの幅を 1200px 程度と 500px 程度の両方で 2〜4 を繰り返す。

**期待結果**

- 上のブロック（名前・段位・ポイント・進捗バー・残pt）は、条件の有無にかかわらず**まったく同じ高さ・同じ位置**に見える。
- 増減するのは下の「段位の詳細」カードの中身だけである。

**問い**（合否と別に書いてください）: 「ヒーローの高さは変わらず、層側のカードだけが増減する」という挙動を、実際に見てどう感じましたか。落ち着いて見えますか、それとも情報が離れて読みにくいですか。

**結果**

- [ ] 合格
- [ ] 不合格
- [ ] 判断保留

```
実際に見えたもの / 保留の理由 / 上の問いへの回答:

```

---

### V6. 昇降条件がサマリータブでしか見えないことの当否

**操作**

1. 実機スマホで V1 と同じプレイヤーのサマリー画面を開く。
2. 下部のタブで **サマリー → 比較 → スタッツ** と移動し、またサマリーへ戻る。これを何度か往復する。
3. 移動のたびに、上のヒーロー（名前・段位・ポイント）と、サマリーにだけ出る「段位の詳細」カードを見る。

**期待結果**

- 段位・ポイントはどのタブでも見えている。
- 「段位の詳細」（昇降条件・通算試合数）はサマリータブでのみ見える。

**問い**（オーナー決定事項）: 昇降条件がサマリータブでしか見えないことを、実際に使ってみて不便に感じましたか。「サマリーに戻れば見られるので気にならない」／「段位の隣に常に欲しい」のどちらでも構いません。感じたままを書いてください。

**結果**

- [ ] 合格
- [ ] 不合格
- [ ] 判断保留

```
実際に使ってみて / 保留の理由 / 上の問いへの回答:

```

---

## 主観評価項目

> 正解はありません。感じたままを書いてください。「特に何も感じない」も有効な回答です。

### S1. 段位色が大きな数字に乗ったときの印象

**見るもの**: V2 で見た、段位ごとのポイントの数字（一番大きい文字）と★の色。

**問い**: 段位ごとに、この色から受ける印象を書いてください。段位の格の違いが色から伝わるか、伝わらないか。あわせて「LLMに作らせた感」を感じる箇所があれば、どこにどう感じたかを書いてください。

```
回答:

```

---

### S2. カード1全体の情報量と並び順

**見るもの**: V1 または V2 で開いたプレイヤーのサマリー画面の上半分。

**問い**: 最初に目に入ってほしい情報は何だと思いますか。実際に最初に目に入ったものは何でしたか。並び順や文字の大きさで気になる点があれば書いてください。

```
回答:

```

---

### S3. 「フィルタ非適用」の注記

**見るもの**: 注記は2箇所に分かれています。(a) ヒーローの名前の右端にある小さい文字「フィルタ非適用」と、そのすぐ下にあるフィルタのチップ。(b) 「段位の詳細」カードの一番下の「全モード・全期間 通算 N 戦（モード・期間フィルタの影響を受けません）」。

**問い**: (a) だけを見て、フィルタが段位・ポイントに効かないことが伝わりましたか。(b) まで読んで初めて分かりましたか。伝わらなかった場合、どこにどう書かれていれば伝わったと思いますか。

```
回答:

```

---

## 実施環境（記入をお願いします）

| 項目 | 記入欄 |
|---|---|
| OS / バージョン | |
| ブラウザ / バージョン | |
| ディスプレイ（解像度・スケーリング） | |
| スマホ機種 / OS / ブラウザ | |
| OSのダーク/ライト設定 | |

## 総合

- [ ] 全項目合格 — マージ可
- [ ] 要修正あり（下記）
- [ ] 判断が必要な論点あり（下記）

```
統括担当への申し送り:

```
