# UI検証依頼: Issue 8 — アイデンティティカード（段位・pt・昇降条件）

- **依頼日**: 2026-08-24
- **対象ブランチ / コミット**: `feat/issue-8-identity-card` / （PR作成後に記入）
- **所要時間の目安**: 約 10 分
- **なぜ逆発注するか**: 段位タグ（Display Small）の視認性・昇降条件バッジの配色の印象・ローディング中の skeleton アニメーション・順位グラフプレースホルダの見た目は、主観的な評価や実機確認が必要なためエージェントでは確認できない。

## 準備

```bash
git switch feat/issue-8-identity-card
npm install   # 不要の場合もあり
npm run dev
```

ブラウザで `http://localhost:5173/#/4/player/<実在プレイヤーID>/summary` を開く。  
（実在プレイヤーIDは amae-koromo の検索画面から取得してください）

> 終わったら dev サーバーは Ctrl+C で停止してください。

---

## 検証項目

### V1. 段位タグ（大きな数値）の視認性

**操作**
1. プレイヤーページ（サマリータブ）を開き、ヒーロー領域（薄い背景色の上部）を見る
2. 段位タグ（例: 「雀傑2」「雀聖1」などの大きな文字）を確認する

**期待結果**
- 段位タグが Display Small（約 36px）で大きく表示されている
- その隣/下に pt 形式（例: `雀傑2 232/1400`）が Headline Large（約 32px）で並んでいる
- 文字が背景色（`--md-sys-color-surface-container-high`）と十分コントラストを持っている

**結果**（← ここを埋めてください）

- [ ] 合格
- [X] 不合格
- [ ] 判断保留

```
実際に見えたもの / 保留の理由:
「雀傑2」の表示が2つ出ている
```

---

### V2. 昇降条件バッジの色と視認性

**操作**
1. ヒーロー領域の中の「昇格」「降格」ラベルとバッジ（例: 「1位」「2位 84,100点以上」など）を確認する
2. ライトモードで確認後、`localStorage.setItem('mjsv:color-mode', 'dark')` を入力してリロードし、ダークモードでも確認する

**期待結果**
- 昇格バッジ: 青みがかった背景（primary-container）に暗い文字（on-primary-container）
- 降格バッジ: 赤みがかった背景（error-container）に暗い文字（on-error-container）
- ライト/ダーク両方で文字が読みやすい

**結果**

- [X] 合格
- [ ] 不合格
- [ ] 判断保留

```
実際に見えたもの / 保留の理由:

```

---

### V3. ローディング中の Skeleton 表示

**操作**
1. ブラウザの DevTools でネットワークスピードを「Slow 3G」に変更する
2. ページをリロードし、データ読み込み中のヒーロー領域を観察する

**期待結果**
- データ読み込み中はグレーのブロックが 3 段（名前・段位タグ・pt）表示される
- ブロックが緩やかにパルスアニメーション（フェードイン/アウト）する
- 読み込み完了後に実際のコンテンツに切り替わる

**注意**: ブラウザのタブが背面（非表示）だとアニメーションが停止する場合があります（ブラウザ仕様）。タブをアクティブにした状態で確認してください。

**結果**

- [X] 合格
- [ ] 不合格
- [ ] 判断保留

```
実際に見えたもの / 保留の理由:

```

---

### V4. 順位グラフのプレースホルダ枠

**操作**
1. ヒーロー領域の最下部（昇降条件バッジの下）を見る
2. 破線の枠で囲まれた「順位グラフ（近日実装予定）」というエリアを確認する

**期待結果**
- 最低高さ 64px の破線枠が表示されている
- 「未実装エリア」として視覚的に明確（実装済みのコンテンツと誤解しない）

**結果**

- [ ] 合格
- [ ] 不合格
- [X] 判断保留

```
実際に見えたもの / 保留の理由:
順位グラフはヒーロー領域の背景に表示させようかと思っています（Steamのダウンロード中表示みたいな）
```

---

### V5. スクロール時の sticky hero 追随

**操作**
1. プレイヤーページを開き、コンテンツが十分あることを確認する（サマリータブのカードプレースホルダ4枚が表示されている）
2. 画面を下にスクロールする

**期待結果**
- ヒーロー領域（段位・pt・昇降条件・フィルタバー）が画面上部に固定され続ける
- スクロールするとカードリスト（薄い丸角の灰色カード）がヒーロー領域の上に重なってせり上がる

**結果**

- [ ] 合格
- [ ] 不合格
- [X] 判断保留

```
実際に見えたもの / 保留の理由:
Issue 5のUIチェックシート参照。
```

---

## 主観評価項目

> 正解はありません。感じたままを書いてください。「特に何も感じない」も有効な回答です。

### S1. アイデンティティ表示全体の印象

**見るもの**: ヒーロー領域全体（段位タグ・pt・残pt・昇降条件）

**問い**: 「プレイヤーの段位と現状が一目でわかる」という目標に対して、現在の表示はどう感じますか？特に改善したい点や気になった点を書いてください。

```
回答:
名前の方が大きくあるべきです。
また、段位ポイントをプログレスバーのようにグラフで表示させるのはどうでしょうか？
```

---

### S2. 昇格・降格バッジの表現の分かりやすさ

**見るもの**: 「昇格」「降格」ラベルとバッジの組み合わせ

**問い**: 「昇格条件」「降格条件」の意味が直感的に伝わりますか？例えば「あと1位を取れば昇段できる」という意味が読み取れるか、など感じたことを書いてください。

```
回答:
スコープ外ですが、昇格条件・降格条件は過去のスクリプトを参考に修正が必要です。
場所→ ../../old/251115_amae-kokoro/03_works/251123_mdcli/
```

---

## 実施環境（記入をお願いします）

| 項目 | 記入欄 |
|---|---|
| OS / バージョン | macOS 26.6 |
| ブラウザ / バージョン | Google Chrome 151.0.7922 |
| ディスプレイ（解像度・スケーリング） | 1920 * 1280 |
| OSのダーク/ライト設定 | ダーク |
## 総合

- [ ] 全項目合格 — マージ可
- [ ] 要修正あり（下記）
- [X] 判断が必要な論点あり（下記）

```
統括担当への申し送り:
上記記載の通り。
```
